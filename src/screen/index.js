import React, { Component } from "react";
import {
    SafeAreaView,
    View,
    Text,

} from "react-native";
import Login from "./login";

export default RootComponent = function () {
    return(
        <SafeAreaView>
            <Login />
        </SafeAreaView>
    );
}